import { HorizontalItemCard } from './HorizontalItemCard.jsx';

/** Dashboard / profile report row — horizontal card that opens item details. */
export function ItemCard({ item }) {
  return <HorizontalItemCard item={item} />;
}
