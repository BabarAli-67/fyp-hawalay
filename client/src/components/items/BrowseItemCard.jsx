import { HorizontalItemCard } from './HorizontalItemCard.jsx';

/** Community browse feed — horizontal card linking to item details. */
export function BrowseItemCard({ item }) {
  return <HorizontalItemCard item={item} />;
}
