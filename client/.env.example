# Client (Vite) — copy to `.env` and adjust.
# API base URL for axios (no trailing slash required).
VITE_API_URL=http://localhost:5000

# Optional: Google Identity Services client ID for @react-oauth/google.
# Leave unset until you have a client ID from Google Cloud Console.
# VITE_GOOGLE_CLIENT_ID=
