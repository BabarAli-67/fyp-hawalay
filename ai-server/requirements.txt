fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-dotenv>=1.0.0,<2.0
easyocr>=1.7.0,<2.0
google-genai>=1.0.0,<2.0
motor>=3.6.0,<4.0
python-multipart>=0.0.9,<1.0
pydantic>=2.0.0,<3.0
pydantic-settings>=2.0.0,<3.0

# Shared ML utilities (aligned with object_v1 training stack)
numpy==2.3.5
Pillow==12.0.0

# OCR / YOLO (region detection + OpenCV preprocessing; torch installed via ultralytics)
opencv-python-headless==4.10.0.84
ultralytics>=8.3.0,<9.0

# object_v1 — InceptionV3 Keras classifier (friend's training stack, ~6 months ago)
# Use Python 3.11–3.12 (TensorFlow 2.20 / numpy 2.3.x; no 3.13+ wheels yet).
# Training-only deps (not needed at runtime): scikit-learn, matplotlib, torch/torchvision.
tensorflow==2.20.0
keras==3.12.0
